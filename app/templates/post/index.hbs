Post visualization

{{partial 'post/show'}}

{{#if isMine}}
  <div class="controls">
    <ul>
      <li>
        {{#link-to 'post.edit'}}Edit{{/link-to}}
      </li>
      <li>
        {{#link-to 'post.delete'}}Delete{{/link-to}}
      </li>
    </ul>
  </div>

  {{outlet}}
{{/if}}
